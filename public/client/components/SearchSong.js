var SearchSong = () => {
	//need to inherit song info from parent
	//two ways of making the song model - as a link and as a button
	return (
		<a href="#" class="search-song list-group-item">{/*props.name?*/}</a>
		<button type="button" class="search-song list-group-item">{/*props.name?*/}</button>
	)
}